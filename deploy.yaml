---
- name: Deploy
  hosts: docker
  remote_user: ubuntu
  tasks:
  - name: Pull default Docker image
    docker_image:
      name: ccza/capstone

  - name: Create default containers
    docker_container:
      name: capstone
      image: ccza/capstone
      state: started
